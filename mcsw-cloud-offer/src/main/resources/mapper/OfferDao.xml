<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="mcsw.offer.dao.OfferDao">

    <resultMap type="mcsw.offer.entity.Offer" id="OfferMap">
        <result property="id" column="id" jdbcType="INTEGER"/>
        <result property="company" column="company" jdbcType="VARCHAR"/>
        <result property="city" column="city" jdbcType="VARCHAR"/>
        <result property="salary" column="salary" jdbcType="VARCHAR"/>
        <result property="type" column="type" jdbcType="INTEGER"/>
        <result property="education" column="education" jdbcType="VARCHAR"/>
        <result property="industry" column="industry" jdbcType="INTEGER"/>
        <result property="college" column="college" jdbcType="INTEGER"/>
        <result property="remarks" column="remarks" jdbcType="VARCHAR"/>
        <result property="createTime" column="create_time" jdbcType="TIMESTAMP"/>
        <result property="updateTime" column="update_time" jdbcType="TIMESTAMP"/>
        <result property="userId" column="user_id" jdbcType="INTEGER"/>
        <result property="major" column="major" jdbcType="VARCHAR"/>
        <result property="positionName" column="position_name" jdbcType="VARCHAR"/>
    </resultMap>

    <!-- 批量插入 -->
    <insert id="insertBatch" keyProperty="id" useGeneratedKeys="true">
        insert into mcsw.offer(company, city, salary, type, education, industry, college, remarks, create_time, update_time, user_id, major, position_name)
        values
        <foreach collection="entities" item="entity" separator=",">
        (#{entity.company}, #{entity.city}, #{entity.salary}, #{entity.type}, #{entity.education}, #{entity.industry}, #{entity.college}, #{entity.remarks}, #{entity.createTime}, #{entity.updateTime}, #{entity.userId}, #{entity.major}, #{entity.positionName})
        </foreach>
    </insert>
    <!-- 批量插入或按主键更新 -->
    <insert id="insertOrUpdateBatch" keyProperty="id" useGeneratedKeys="true">
        insert into mcsw.offer(company, city, salary, type, education, industry, college, remarks, create_time, update_time, user_id, major, position_name)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.company}, #{entity.city}, #{entity.salary}, #{entity.type}, #{entity.education}, #{entity.industry}, #{entity.college}, #{entity.remarks}, #{entity.createTime}, #{entity.updateTime}, #{entity.userId}, #{entity.major}, #{entity.positionName})
        </foreach>
        on duplicate key update
         company = values(company) , city = values(city) , salary = values(salary) , type = values(type) , education = values(education) , industry = values(industry) , college = values(college) , remarks = values(remarks) , create_time = values(create_time) , update_time = values(update_time) , user_id = values(user_id) , major = values(major) , position_name = values(position_name)     </insert>

</mapper>

