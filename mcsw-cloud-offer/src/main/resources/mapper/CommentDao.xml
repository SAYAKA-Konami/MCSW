<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="mcsw.offer.dao.CommentDao">

    <resultMap type="mcsw.offer.entity.Comment" id="CommentMap">
        <result property="id" column="id" jdbcType="INTEGER"/>
        <result property="mcswId" column="mcsw_id" jdbcType="VARCHAR"/>
        <result property="parentId" column="parent_id" jdbcType="INTEGER"/>
        <result property="createTime" column="create_time" jdbcType="TIMESTAMP"/>
        <result property="updateTime" column="update_time" jdbcType="TIMESTAMP"/>
        <result property="content" column="content" jdbcType="VARCHAR"/>
        <result property="userId" column="user_id" jdbcType="INTEGER"/>
    </resultMap>

    <!-- 批量插入 -->
    <insert id="insertBatch" keyProperty="id" useGeneratedKeys="true">
        insert into mcsw.comment(mcsw_id, parent_id, create_time, update_time, content, user_id)
        values
        <foreach collection="entities" item="entity" separator=",">
        (#{entity.mcswId}, #{entity.parentId}, #{entity.createTime}, #{entity.updateTime}, #{entity.content}, #{entity.userId})
        </foreach>
    </insert>
    <!-- 批量插入或按主键更新 -->
    <insert id="insertOrUpdateBatch" keyProperty="id" useGeneratedKeys="true">
        insert into mcsw.comment(mcsw_id, parent_id, create_time, update_time, content, user_id)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.mcswId}, #{entity.parentId}, #{entity.createTime}, #{entity.updateTime}, #{entity.content}, #{entity.userId})
        </foreach>
        on duplicate key update
         mcsw_id = values(mcsw_id) , parent_id = values(parent_id) , create_time = values(create_time) , update_time = values(update_time) , content = values(content) , user_id = values(user_id)     </insert>

</mapper>

